# Sample environment variables (rename to .env and fill in your values)

# Server Configuration
PORT=5001                       # Port for Express server
NODE_ENV=development            # Environment (development/production)

# Local Repository Paths 
# These should point to local filesystem paths where your repositories are stored on the server
# The application analyzes these repositories through server-side API calls
DEV_REPO_PATH=/path/to/your/dev/repo       # Development path for repo
PROD_REPO_PATH=/path/to/your/production/repo # Production path for repo
EXTRA_REPO_PATH=/path/to/your/repo         # Optional repo paths
EXTRA_REPO_PATH_2=/path/to/your/repo       # Optional repo paths

# API Keys (do not include actual values in any committed files)
API_KEY=your_secure_api_key_here           # Used in Bearer token authentication for protected API endpoints

# ───────────────────────────────────────────────────────────────────────────────
# NGINX CONFIG EXAMPLE WITH BEARER TOKEN (Proxy to Express backend)
# ───────────────────────────────────────────────────────────────────────────────
# Note: In production, configure nginx to proxy API requests to the Express server
# Also include the Authorization header (e.g., Bearer token)

# location /api/ {
#   proxy_pass http://localhost:5001;
#   proxy_http_version 1.1;
#   proxy_set_header Upgrade $http_upgrade;
#   proxy_set_header Connection 'upgrade';
#   proxy_set_header Host $host;
#   proxy_cache_bypass $http_upgrade;
#   proxy_set_header Authorization $http_authorization;  # Allow Bearer tokens through
# }
